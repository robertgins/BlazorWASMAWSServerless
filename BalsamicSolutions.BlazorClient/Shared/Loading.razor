<div class="loading-container">
    <span class="loader">@Message</span>
</div>

@code {
    [Parameter]
    public string Message { get; set; }

    protected override void OnInitialized()
    {
        base.OnInitialized();
        Message = Message == string.Empty || Message == null ? "Loading" : Message;
    }
}
